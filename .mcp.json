{
  "mcpServers": {
    "redmine": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PROJECT_DIR:-.}/server/redmine.js"],
      "env_vars": ["REDMINE_URL", "REDMINE_API_KEY", "REDMINE_CONFIG_PATH"]
    },
    "markdown-viewer": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PROJECT_DIR:-.}/server/markdown-viewer.js"]
    }
  }
}
